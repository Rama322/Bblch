import { bot } from '../../../index.js';

const searchCommand = async (ctx, next) => {
  await ctx.deleteMessage();
  await ctx.reply('<b>Введите текст:</b>', {
    parse_mode: 'HTML',
  });
  const reply = ctx.message.text;
  console.log(reply);
};

export { searchCommand };
